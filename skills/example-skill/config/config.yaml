model:
  name: "example-model"
  max_length: 512
  temperature: 0.7

data:
  input_path: "data/input.json"
  output_path: "data/output.json"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"